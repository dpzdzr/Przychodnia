<UserControl x:Class="Przychodnia.View.LaboratoryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Przychodnia.View"
             xmlns:shared="clr-namespace:Przychodnia.ViewModel.Shared" 
             d:DataContext="{d:DesignInstance Type=shared:LaboratoryListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid d:Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Lista laboratoriów" FontSize="15" Margin="0,0,0,5" FontWeight="DemiBold"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Labs}" SelectedItem="{Binding SelectedLab}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserReorderColumns="False" HeadersVisibility="Column" FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nazwa"  Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Kierownik" Binding="{Binding ManagerFullName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Grid.Row="2" Content="Usuń" Margin="0,10,0,0" Command="{Binding RemoveButtonCommand}" HorizontalAlignment="Left"/>
            </Grid>

            <Grid Grid.Row="1" Margin="10" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Lista pracowników" FontSize="15" Margin="0,25,0,5" FontWeight="DemiBold" />
                <DataGrid Grid.Row="1" ItemsSource="{x:Null}" SelectedItem="{x:Null}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserReorderColumns="False" HeadersVisibility="Column" FontSize="12" Margin="0,0,0,60">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imię"  Binding="{x:Null}" Width="*"/>
                        <DataGridTextColumn Header="Nazwisko" Binding="{x:Null}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
        
        <Border Grid.Column="1" CornerRadius="10" BorderThickness="2" BorderBrush="LightGray" Margin="25" >
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="1">
                    <TextBlock d:Text="Dodaj nowe laboratorium" Text="{Binding FormHeaderText}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25" FontWeight="DemiBold" TextWrapping="Wrap"/>
                </Grid>
                <Grid Grid.Row="2" Margin="0,30,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Nazwa" Margin="0,0,0,2" FontSize="13"/>
                    <TextBox Grid.Row="1" Grid.Column="1" FontSize="17" Text="{Binding EditLab.Name}"/>
                </Grid>
                <Grid Grid.Row="3" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Typ" Margin="0,0,0,2" FontSize="13"/>
                    <TextBox Grid.Row="1" Grid.Column="1" FontSize="17" Text="{Binding EditLab.Type}"/>
                </Grid>
                <Grid Grid.Row="4" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Kierownik laboratorium" Margin="0,0,0,2" FontSize="13"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" FontSize="13" ItemsSource="{Binding Managers}" 
                              DisplayMemberPath="FullName"
                              SelectedItem="{Binding EditLab.Manager}"/>
                </Grid>
                <Grid Grid.Row="5" Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Content="{Binding ActionButtonText}" Command="{Binding ActionButtonCommand}"/>
                    <Button Grid.Row="1" Content="Anuluj" Command="{Binding CancelButtonCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
