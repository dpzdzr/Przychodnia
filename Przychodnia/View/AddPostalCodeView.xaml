<UserControl x:Class="Przychodnia.View.AddPostalCodeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Przychodnia.View" 
        xmlns:shared="clr-namespace:Przychodnia.ViewModel.Shared"
        d:DataContext="{d:DesignInstance Type=shared:AddPostalCodeViewModel}"
             mc:Ignorable="d"
       d:Width="600" d:Height="400">
    
    <Grid d:Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="Dodaj nowy kod pocztowy" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25" FontWeight="DemiBold"/>
        
        <StackPanel Grid.Column="1" Grid.Row="2">
            <TextBlock Text="Miasto" Margin="0,0,0,0"/>
            <TextBox Text="{Binding City}"/>
            <TextBlock Text="Kod pocztowy" Margin="0,10,0,0"/>
            <toolkit:MaskedTextBox Mask="00-000" Text="{Binding PostalCode}"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Dodaj" Grid.Column="1" Width="150" Height="25" Margin="20" Command="{Binding SaveButton}"/>
    </Grid>
</UserControl>
