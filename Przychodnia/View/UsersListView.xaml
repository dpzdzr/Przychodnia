<UserControl x:Class="Przychodnia.View.UsersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Przychodnia.View"
             mc:Ignorable="d"
             xmlns:viewmodel="clr-namespace:Przychodnia.ViewModel"
             xmlns:converter="clr-namespace:Przychodnia.Converter"
             d:DataContext="{d:DesignInstance Type=viewmodel:UsersListViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>
    </UserControl.Resources>
    
    <Grid d:Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Users}" Grid.Row="0" AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserDeleteRows="False" CanUserAddRows="False" ColumnWidth="*" Margin="10" IsReadOnly="True" SelectedItem="{Binding SelectedUser}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Imię" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Nazwisko" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding UserType.Name}"/>
                <DataGridTextColumn Header="Login" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Hash hasła" Binding="{Binding PasswordHash}"/>
                <DataGridTextColumn Header="Numer licencji" Binding="{Binding LicenseNumber}"/>
                <DataGridTextColumn Header="Identyfikator laboratorium" Binding="{Binding LaboratoryId}"/>
                <DataGridTextColumn Header="Aktywny" Binding="{Binding IsActive, Converter={StaticResource BoolToYesNoConverter}}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Content="Edytuj" Grid.Column="0" Width="120" Height="35" Margin="5" IsEnabled="{Binding CanExecute}"/>
            <Button Content="Usuń" Grid.Column="1" Width="120" Height="35" Margin="5" Command="{Binding DeleteUserCommand}" IsEnabled="{Binding CanExecute}"/>
        </Grid>
    </Grid>
</UserControl>
