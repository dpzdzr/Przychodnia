<UserControl x:Class="Przychodnia.View.PatientFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Przychodnia.View"
             xmlns:viewmodel="clr-namespace:Przychodnia.ViewModel.Shared"
             xmlns:converter="clr-namespace:Przychodnia.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodel:PatientAddViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converter:SexToStringConverter x:Key="SexToStringConverter"/>
    </UserControl.Resources>
    
    <Grid d:Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Text="{Binding HeaderText}" Margin="20" Grid.ColumnSpan="2" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25" FontWeight="Bold"/>
        <StackPanel Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <TextBlock Text="Imię" Margin="0,0,0,0"/>
            <TextBox Text="{Binding FormData.FirstName}"/>
            <TextBlock Text="Nazwisko" Margin="0,5,0,0"/>
            <TextBox Text="{Binding FormData.LastName}"/>
            <TextBlock Text="PESEL" Margin="0,5,0,0"/>
            <TextBox Text="{Binding FormData.Pesel}"/>
            <TextBlock Text="Płeć" Margin="0,5,0,0"/>
            <ComboBox ItemsSource="{Binding SexDispDict}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding FormData.Sex}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top" Width="250">
            <TextBlock Text="Ulica" Margin="0,5,0,0"/>
            <TextBox Text="{Binding FormData.Street}"/>
            <TextBlock Text="Nr domu" Margin="0,5,0,0"/>
            <TextBox Text="{Binding FormData.HouseNumber}"/>
            <TextBlock Text="Nr mieszkania" Margin="0,5,0,0"/>
            <TextBox Text="{Binding FormData.ApartmentNumber}"/>
            <TextBlock Text="Kod pocztowy" Margin="0,5,0,0"/>
            <ComboBox 
                ItemsSource="{Binding PostalCodes}"
                IsEditable="True"
                IsTextSearchEnabled="False"
                SelectedItem="{Binding FormData.PostalCode}"
                DisplayMemberPath="Code"
                Text="{Binding EnteredCode, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <Button
            Grid.Row="3" Grid.ColumnSpan="2" Content="{Binding ActionButtonText}" 
            Width="150" Height="25" Margin="0,30,0,0" Command="{Binding ActionButtonCommand}"/>
    </Grid>
</UserControl>
