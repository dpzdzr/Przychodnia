<UserControl x:Class="Przychodnia.View.PostalCodesListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Przychodnia.View" 
        xmlns:shared="clr-namespace:Przychodnia.ViewModel.Shared"
        d:DataContext="{d:DesignInstance Type=shared:PostalCodesListViewModel}"
             mc:Ignorable="d"
       d:Width="600" d:Height="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    
    <Grid d:Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,15,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <DataGrid ItemsSource="{Binding PostalCodes}" SelectedItem="{Binding SelectedPostalCode}" AutoGenerateColumns="False" Margin="5" IsReadOnly="True" CanUserAddRows="False" CanUserReorderColumns="False" HeadersVisibility="Column" FontSize="15">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kod pocztowy"  Binding="{Binding Code}" Width="*"/>
                    <DataGridTextColumn Header="Miasto" Binding="{Binding City}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Usuń" Grid.Column="0" Margin="5,10,5,10" Height="25" Command="{Binding DeleteCommand}"/>
            </Grid>

        </Grid>

        <Grid Grid.Column="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="1" Grid.ColumnSpan="3" d:Text="Dodaj nowy kod pocztowy" Text="{Binding FormHeader}" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25" TextWrapping="Wrap" TextAlignment="Center" FontWeight="DemiBold"/>

            <StackPanel Grid.Column="1" Grid.Row="2">
                <TextBlock Text="Miasto" Margin="0,0,0,0"/>
                <TextBox Text="{Binding City}"/>
                <TextBlock Text="Kod pocztowy" Margin="0,10,0,0"/>
                <toolkit:MaskedTextBox Mask="00-000" Text="{Binding PostalCode}"/>
            </StackPanel>

            <Button Grid.Row="3" Content="{Binding ActionButtonText}" Grid.Column="1" Width="150" Height="25" Margin="20,20,20,10" Command="{Binding SaveCommand}"/>
            <Button Grid.Row="4" Content="Anuluj" Grid.Column="1" Width="150" Height="25" Margin="20, 10, 20, 20" Command="{Binding CancelCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
